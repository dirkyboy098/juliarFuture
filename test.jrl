class node = {
    int data;
	function foo() = {
		printLine("called from inside node::foo");
		availableMemory();
	}
}

function main() = {

    printLine("before do while");

    do {
        delete 3;
   	    printLine( "foo" );
        node one;
	    one::foo();
	    foo1();
	}
	while (true)

	printLine( "after do while" );
    one::data;
    printLine( "" );
}


function foo1() = {
    printLine( "" );
}
