function main() = {
    int x1 = 4;

    while ( x1 == 4 ){
        printLine("while loop");
        x1 = + x1 1;
    }

    int xy = + 1 3 10 20 40;
    printLine( "1 + 3 + 10 + 20 + 40 = ");
    printLine(xy );

    int t = test(   );
    printLine(t);
    printLine("calling an if statement with true");

    if (true){
        printLine("this is an if statement");
        int z = 3;
        int y = 4;
        printLine(y);
    }

    printLine("calling an if statement with false");

    if (false){
        int x = test();
        boolean b = x == 3;
        printLine(b);
    }
}

function test() = {
    printLine("function test");
    int t = foo();
    printLine(t);
    printLine("test method");
    printLine("end function test");
    int y = + 100 200 3300;
    return y;
}


function foo() = {
    int x = + 1 2;
    return x;
}


function testPrimitives() = {
    int x = 400;
    fileOpen ("test.jrl");
    printLine("about to print variable x");
    printLine( x );
    printLine("printed the value of variable 'x'");
}
