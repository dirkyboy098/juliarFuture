class node = {
    int data;
	function foo() = {
		printLine("called from inside node::foo");
		availableMemory();
	}
}

function main() = {

    printLine("before do while");
    int x;

    do {
        delete 3;
   	    printLine( "foo" );
        node one;
	    one::foo();
	    foo1();
	    ++x;
	}
	while (x < 3)

	printLine( "after do while" );
    one::data;
    printLine( "" );
}


function foo1() = {
    printLine( "" );
}
