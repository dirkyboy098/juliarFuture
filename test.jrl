function main() = {
    string file = fileOpen("./test.jrl");
    parse(file);
}


function parse(string s) = {
    printLine(" ========== Print value of variable passed in from main funciton ============");
    printLine(s);
}